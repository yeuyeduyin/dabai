类加载器的作用就是把class文件加载进JVM虚拟机中，但是JVM启动是并不会把所有的class文件一次性都加载，而是根据需要去动态加载。
class文件是字节码文件，是jvm虚拟机可以识别的文件。
java自带三个类加载器：
Bootstrap ClassLoader-最顶层的类加载器
Extention ClassLoader-扩展的类加载器
Appclass Loader-也称为SystemAppClass加当前应用的classpath的所有类
